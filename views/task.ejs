<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LuvTheTask - Add Task</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel = "stylesheet" type = "text/css" href = "/stylesheets/task.css">
</head>
<body>
    <div class="floating-elements">
    </div>
    
    <div class="container">
        <header>
            <div class="brand">
                <div class="brand-logo">
                    <i class="fas fa-heart"></i>
                </div>
                <h1 class="brand-name">LuvTheTask</h1>
            </div>
            
            <div class="nav-buttons">
                <button class="btn btn-outline" id="viewTasksBtn">
                    <a href = "/alltask">
                    <i class="fas fa-tasks"></i> View Tasks
                    </a>
                </button>
            </div>
        </header>
        
        <section class="main-content">
            <h1 class="page-title">Add New Task</h1>
            
            <div class="task-form-container">
                <form action = "/task" method = "post" id="taskForm">
                    <div class="form-group">
                        <label class="form-label" for="taskTitle">
                            <i class="fas fa-heading"></i> Task Title
                        </label>
                        <input 
                            name = "title"
                            type="text" 
                            id="taskTitle" 
                            class="form-input" 
                            placeholder="Enter a descriptive title for your task..."
                            required
                        >
                    </div>
                    
                    <div class="form-group">
                        <label class="form-label" for="taskDescription">
                            <i class="fas fa-align-left"></i> Task Description
                        </label>
                        <textarea 
                            name = "detail"
                            id="taskDescription" 
                            class="form-input" 
                            placeholder="Provide details about your task. What needs to be done? Any specific requirements?"
                            required
                        ></textarea>
                    </div>
                    
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-plus-circle"></i> Add Task
                        </button>
                    </form>
                        <button type="button" class="btn btn-secondary" id="previewTaskBtn">
                            <a href = "/alltask">
                            <i class="fas fa-eye"></i> View Task
                            </a>
                        </button>
                    </div>
            </div>
        </section>
    </div>
    <script src = "/javascripts/task.js"></script>
</body>
</html>